#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char const *argv[])
{
    string user_name;

    // num_tries(0) 表示使用构造函数初始化一个int数值
    int user_value, num_tries(0), num_right = 0; 
    double user_score = 0.0;
    char user_more;
    bool go_for_it = true;
    const int MAX_TRY = 3;
    const int SEQ_SIZE = 18;
    int pell_seq[SEQ_SIZE];
    vector<int> pell_vector(SEQ_SIZE);
    return 0;
}
